# Deploying Software-Defined Access (SDA) Fabrics
Contains code to perform pre-SDA setup along with SDA-specific operations,
including `data_ref/` examples and `input_data/` files consumed by several
of the scripts.
